// When flying to various points, it's helpful to
// center on a point that's slightly further south
// so that the actual point the user cares about is
// in the top third of the screen. This prevents
// the active pane from covering up whatever the
// user is flying to see.
export const LAT_OFFSET=0.007
